<Window x:Class="CC.ImageOverlay.Views.ColorPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="カラーピッカー"
        Height="400" Width="350"
        WindowStyle="ToolWindow"
        WindowStartupLocation="CenterOwner"
        Background="#0A1929">
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Preview -->
        <Border Grid.Row="0" Height="60" CornerRadius="8" Margin="0,0,0,16"
                BorderBrush="#1E3A5F" BorderThickness="1">
            <Border.Background>
                <SolidColorBrush x:Name="PreviewBrush" Color="White"/>
            </Border.Background>
        </Border>

        <!-- Color Palette -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="30"/>
            </Grid.ColumnDefinitions>

            <!-- Hue/Saturation -->
            <Border Grid.Column="0" x:Name="ColorCanvas" CornerRadius="4"
                    MouseLeftButtonDown="ColorCanvas_MouseDown"
                    MouseMove="ColorCanvas_MouseMove">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="White" Offset="0"/>
                        <GradientStop x:Name="HueColor" Color="Red" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Border CornerRadius="4">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="Transparent" Offset="0"/>
                            <GradientStop Color="Black" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Canvas>
                        <Ellipse x:Name="ColorSelector" Width="14" Height="14" 
                                 Stroke="White" StrokeThickness="2"
                                 Canvas.Left="0" Canvas.Top="0"/>
                    </Canvas>
                </Border>
            </Border>

            <!-- Hue Slider -->
            <Border Grid.Column="1" Margin="8,0,0,0" CornerRadius="4"
                    x:Name="HueSlider"
                    MouseLeftButtonDown="HueSlider_MouseDown"
                    MouseMove="HueSlider_MouseMove">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#FF0000" Offset="0"/>
                        <GradientStop Color="#FFFF00" Offset="0.17"/>
                        <GradientStop Color="#00FF00" Offset="0.33"/>
                        <GradientStop Color="#00FFFF" Offset="0.5"/>
                        <GradientStop Color="#0000FF" Offset="0.67"/>
                        <GradientStop Color="#FF00FF" Offset="0.83"/>
                        <GradientStop Color="#FF0000" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Canvas>
                    <Border x:Name="HueSelector" Height="4" Width="30"
                            Background="White" Canvas.Top="0"/>
                </Canvas>
            </Border>
        </Grid>

        <!-- Alpha Slider -->
        <StackPanel Grid.Row="2" Margin="0,0,0,16">
            <TextBlock Text="透明度 (Alpha)" Foreground="#90CAF9" FontSize="12" Margin="0,0,0,8"/>
            <StackPanel Orientation="Horizontal">
                <Slider x:Name="AlphaSlider" Minimum="0" Maximum="255" Value="255"
                        Width="230" ValueChanged="AlphaSlider_ValueChanged"/>
                <TextBlock x:Name="AlphaText" Text="255" Foreground="White"
                           Margin="12,0,0,0" Width="40"/>
            </StackPanel>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="キャンセル" Click="Cancel_Click"
                    Background="#1E3A5F" Foreground="White"
                    Padding="16,8" Margin="0,0,8,0"/>
            <Button Content="OK" Click="OK_Click"
                    Background="#00B4D8" Foreground="White"
                    Padding="24,8"/>
        </StackPanel>
    </Grid>
</Window>
